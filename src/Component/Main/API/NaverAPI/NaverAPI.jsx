import React from 'react';
import Header from '../Header/Header';

const NaverAPI = () => {
    return (
        <div>
            <Header title={'네이버 API 키등록'} />
        </div>
    );
};

export default NaverAPI;
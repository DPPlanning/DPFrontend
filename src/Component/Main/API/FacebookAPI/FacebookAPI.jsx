import React from 'react';
import Header from '../Header/Header';

const FacebookAPI = () => {
    return (
        <div>
            <Header title={'페이스북 API 키등록'} />
        </div>
    );
};

export default FacebookAPI;